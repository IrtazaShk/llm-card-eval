# llm-card-eval
The LLM-as-judge evaluation pipeline for AI-generated greeting card output. Scores cards across 4 weighted dimensions — prompt fidelity, tone consistency, text accuracy &amp; style coherence. Regression tracking across runs. Runs offline, No API key needed.
